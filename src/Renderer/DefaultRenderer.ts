module renderer {
    export class DefaultRenderer extends es.Renderer {
        constructor() {
            super(0);
        }

        public render(scene: es.Scene) {
            
        }
    }
}